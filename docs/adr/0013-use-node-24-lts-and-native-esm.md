# Use Node.js 24 LTS and native ESM

The gateway package will require Node.js 24 or newer and publish native ESM only. The ACP TypeScript SDK is itself an ESM package, and Node.js 24 is an LTS release line. The project will not maintain a parallel CommonJS build.
